<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mapper.channelMapper">

    <select id="selectByChannelID" resultType="POJO.news">
        select ID,channelID,title,cover,description,postedTime from news where channelID =#{channelID} limit #{limit} offset #{offset};
    </select>

</mapper>